# model-uptime 首次启动自动生成的默认配置。
# 完整示例见项目根目录 config.example.yaml。
# 配置页修改会原子写回此文件；也可直接编辑后重启生效。
admin_token: ""

page:
  title: "model-uptime // status"
  subtitle: "model-uptime"
  probe_comment: "model-uptime service monitor · probing every 60s"
  history_len: 60
  refresh_sec: 5
  show_uptime: true
  show_samples: true
  show_latency: true
  show_avg_load: true

# LLM services use streaming SSE probes by default. Set stream: false per service for synchronous JSON compatibility.
services: []
