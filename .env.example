# 复制为 .env 后按需修改。docker compose 会自动读取。

# 配置页访问令牌（推荐用环境变量而非配置文件，避免密钥入库/镜像层）
ADMIN_TOKEN=change-me

# 服务监听端口（容器内端口，宿主映射见 docker-compose.yml）
PORT=8080

# 数据目录（SQLite 数据库与 config.yaml 所在目录，容器内路径）
DATA_DIR=/data
